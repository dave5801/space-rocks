{% extends "layout.jinja2" %}

{% block content %}
    {% for neo in Orbits %}
    <div class="clearfix">
    <div class="card-body">
    <h1>Name of NEO : {{neo.name}}</h1>
    <h3>ID #: {{neo.neo_id}}</h3>
    <h4>Orbit Peroid : {{neo.orbit_period}}</h4>
    <h5>Perinelion distance : {{neo.perihelion_dist}}</h5>
    <h5>Aphelion distance : {{neo.aphelion_dist}}</h5>
    <h5>Eccentricity : {{neo. eccentricity}}</h5>
    <a href ="{{neo.url}}" target="_blank" class="btn btn-secondary"><h2>Indepth Orbit Detail &rarr;</h2></a>
<hr>
</div>
</div>
    {% endfor %}
{% endblock %}